# Dependencias para build de ejecutables
nuitka>=2.0
ordered-set>=4.0
zstandard>=0.15

# Alternativas
pyinstaller>=6.0
cx-freeze>=6.0

# Para creación de instaladores
briefcase>=0.3

# Herramientas de empaquetado
appdirs>=1.4
packaging>=20.0

# Para builds Windows desde Linux (opcional)
# wine   # Instalar via sistema, no pip
# mingw-w64  # Instalar via sistema
